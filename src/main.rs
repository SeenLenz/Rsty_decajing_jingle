use std::io;
use std::mem::drop;
use std::io::Write;
use std::fs::ReadDir;

use std::fs::File;
use std::io::BufReader;
use rodio::{Decoder, OutputStream, Sink};

mod file_handling;
mod audio_handling;

fn main() -> io::Result<()> {

    return Ok(());
}
